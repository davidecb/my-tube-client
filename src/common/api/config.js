const config = {
    headers: {
        'Access-Control-Allow-Origin': '*',
        'content-type': 'application/json'
    },
    responseType: 'json',
    withCredentials: false
};

export default config;